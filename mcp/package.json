{
  "name": "finance-hub-laravel-mcp",
  "version": "0.1.0",
  "description": "MCP server for the Laravel finance-hub app — shells out to `docker exec ... php artisan` on the running app container",
  "private": true,
  "license": "UNLICENSED",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^4.4.3"
  }
}
